version: '3.9'

services:
  frontend:
    image: afd-frontend:latest
    container_name: afd-frontend
    restart: unless-stopped
    ports:
      - 4210:4200
    networks:
      - net-afd

networks:
  net-afd:
    driver: bridge